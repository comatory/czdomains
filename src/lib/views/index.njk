{% extends "layout.njk" %}

{% block head %}
  <link rel="stylesheet" href="/styles/home.css">
{% endblock %}

{% block content %}
  {% set navigation = { isHome: true } %}
  {% include "includes/navigation.njk" %}
  <p>
    {{ intlGet("home.about") }}
  </p>
  <p>
    {{ formatMessage(
        intlGet("home.description"), {
          link: "<a href='https://archive.org' target='_blank' rel='noreferrer noopener'>archive.org</a>",
          emStart: "<em>",
          emClose: "</em>"
        }) | safe }}
  </p>
  <h4>FAQ</h4>
  <dl>
    <dt>What is Czechia?</dt>
    <dd>
      It is a small country of approximately 10.5 million inhabitants
      located in central Europe. It's also called Czech Republic, formerly
      it was known as Czechoslovakia (pre 1993). Read more
      <a
        href="https://en.wikipedia.org/wiki/Czech_Republic"
        rel="noreferrer noopener"
        target="_blank"
      >
        here
      </a>
    </dd>
    <dt>What is the story behind TLD</dt>
    <dd>TBD</dd>
  </dl>
  <h4>Attributions</h4>
  <p>
    czdomains is using IP2Location.io <a href=https://www.ip2location.io>IP geolocation</a> web service.
  </p>
  <p>
    The icons come from <a href="https://archive.org/details/html-clipart-CD/HTML_CLIPART/">HTML Clipart by JAL Tech (archive link)</a>.
  </p>
  <hr/>
  <div class="footer">
    <em>Latest addition: {{ lastImport.toLocaleString() if lastImport else "N/A" }}</em>
    <span>
      <form
        action="/language"
        method="POST"
      >
        Language: <input type="submit" class="{{ 'footer__language__button--active' if language === 'en' else 'footer__language__button' }}" value="en" name="language" /> <input type="submit" class="{{ 'footer__language__button--active' if language === 'cz' else 'footer__language__button' }}" value="cz" name="language" />
      </form>
    </span>
  </div>
{% endblock %}
